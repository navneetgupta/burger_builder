import * as actionTypes from "./action";

const initialState = {
  ingredients: null,
  totalPrice: 4
};

const reducers = (state = initialState, action) => {
  return state;
};

export default reducers;
